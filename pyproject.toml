[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "PCST"
version = "1.0.0"
description = "基于 SAM 的 PET/CT 图像全身病灶分割软件"
authors = [
    { name="Jinfr"}
]
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.9.1",
    "numpy>=1.26.0",
    "opencv-python>=4.8.0",
    "onnxruntime>=1.16.0",
    "pydicom>=2.4.3",
    "pypinyin>=0.49.0",
    "SimpleITK>=2.3.0",
    "vtk>=9.2.6",
    "pyyaml>=6.0.1",
]

[dependency-groups]
build = [
    "nuitka>=4.1.2",
]

[project.gui-scripts]
pcst = "pcst.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/pcst"]
